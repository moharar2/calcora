Phase 1 SEO bootstrap is intentionally kept dependency-free. initial-seo.js executes before the React module and applies route-aware title, description, canonical, robots, Open Graph, and Twitter metadata. InitialSeoRuntime refreshes the same metadata after Wouter client-side navigation. Keep this bootstrap as the single early metadata source and avoid adding another independent document.head SEO implementation.